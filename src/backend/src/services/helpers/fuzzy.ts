// 1. Fuzzication - Membership function of input fuzzy set
// 2.Fuzzy inference - Rule base
//  - heartbeat: 60 <= heartbeat <=100
// 3. Defuzzication -> output of system

// interface HIInput {
//     heartbeat: number; // heartbeat

// }
function fuzzy(input) {
  let output = 1;
  console.log(output);

  return output;
}
fuzzy("a")
